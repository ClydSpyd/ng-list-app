import { CreatedTextPipe } from './created-text.pipe';

describe('CreatedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
